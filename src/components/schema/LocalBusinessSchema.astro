---
import { business, verifiedPrices } from '../../config/business';
import { absolute, businessId, websiteId, gbpListingUrl } from '../../config/urls';
import ogImage from '../../assets/kiamo-skin-studio-mission-hills-san-diego.jpg';

const site = Astro.site;
const entityId = businessId(site);
const home = absolute('/', site);

/** Every service carries a price except DMK Level 2 & 3, which is prescribed at
    consultation. It is still listed so the catalogue is complete. */
const services = [
  { name: 'First-Time Client Treatment', url: 'treatments/hydrafacial', priced: 'first-time-client' },
  { name: 'HydraFacial Treatment', url: 'treatments/hydrafacial', priced: 'hydrafacial' },
  { name: 'Dermaplane Facial', url: 'treatments/dermaplaning', priced: 'dermaplane-facial' },
  { name: 'DMK Level 1 Enzyme Treatment', url: 'treatments/dmk-enzyme-therapy', priced: 'dmk-enzyme-1' },
  { name: 'LIRA Gentle Renewal Peel', url: 'treatments/lira-peels', priced: 'lira-light-peel' },
  { name: 'Keravive Scalp Treatment', url: 'treatments/keravive-scalp', priced: 'keravive-scalp' },
  { name: 'Procell Microchanneling', url: 'treatments/procell-microchanneling', priced: 'procell' },
  { name: 'LIRA Advanced Peel', url: 'treatments/lira-peels', priced: 'lira-advanced-peel' },
  { name: 'DMK Level 2 & 3 Enzyme Treatment', url: 'treatments/dmk-enzyme-therapy', priced: null },
] as const;

const schema = {
  '@context': 'https://schema.org',
  '@type': ['MedicalBusiness', 'LocalBusiness', 'HealthAndBeautyBusiness', 'DaySpa'],
  '@id': entityId,
  name: business.name,
  additionalType: 'https://en.wikipedia.org/wiki/Day_spa',
  medicalSpecialty: ['Dermatology', 'Cosmetic Dermatology'],
  description:
    'Expert pigmentation treatment and corrective facials in San Diego. Specializing in hyperpigmentation, melasma, acne scarring, and scalp therapy—safe for all skin tones, including melanin-rich skin.',
  url: home,
  image: new URL(ogImage.src, site).href,
  telephone: business.phone.display,
  email: business.email,
  priceRange: business.priceRange,
  currenciesAccepted: 'USD',
  address: {
    '@type': 'PostalAddress',
    streetAddress: business.address.street,
    addressLocality: business.address.city,
    addressRegion: business.address.region,
    postalCode: business.address.postalCode,
    addressCountry: business.address.country,
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: business.geo.latitude,
    longitude: business.geo.longitude,
  },
  // Ties the on-site entity to the Google Business Profile that ranks in the
  // local pack. Both resolve to the same verified place ID.
  hasMap: gbpListingUrl(business.gbpPlaceId),
  slogan: 'Strategy, correction, intention.',
  // Topics the studio is actually qualified on — the services it performs and
  // the conditions it treats. Nothing here is a claim the site does not already
  // make in prose.
  knowsAbout: [
    'Corrective facials',
    'HydraFacial',
    'HydraFacial Syndeo',
    'HydraFacial Keravive scalp therapy',
    'DMK enzyme therapy',
    'Procell microchanneling',
    'Procell Therapies microchanneling',
    'Chemical peels',
    'LIRA Clinical peels',
    'Dermaplaning',
    'Acne treatment',
    'Acne scarring',
    'Hyperpigmentation',
    'Melasma',
    'Post-inflammatory hyperpigmentation',
    'Skin barrier repair',
    'Melanin-rich skin',
    'Fitzpatrick skin typing',
    'Transepidermal water loss',
  ],
  founder: {
    '@type': 'Person',
    // First name only, and no pronouns anywhere: a surname was never verified
    // and the guard suite fails the build on either.
    name: business.ownerName,
    jobTitle: 'Licensed Esthetician',
    worksFor: { '@id': entityId },
    knowsAbout: [
      'Corrective facials',
      'Melanin-rich skin treatment',
      'Hyperpigmentation',
      'Post-inflammatory hyperpigmentation',
      'Fitzpatrick skin typing',
      'HydraFacial',
      'Procell Therapies microchanneling',
      'LIRA Clinical peels',
      'DMK enzyme therapy',
    ],
    hasCredential: {
      '@type': 'EducationalOccupationalCredential',
      credentialCategory: 'license',
      name: business.ownerCredential,
    },
  },
  areaServed: business.schemaAreaServed.map((name) => ({ '@type': 'Place', name })),
  // Vagaro is request-based; this is the request entry point, not instant booking.
  potentialAction: {
    '@type': 'ReserveAction',
    target: {
      '@type': 'EntryPoint',
      urlTemplate: business.bookingUrl,
      actionPlatform: [
        'http://schema.org/DesktopWebPlatform',
        'http://schema.org/IOSPlatform',
        'http://schema.org/AndroidPlatform',
      ],
    },
    result: { '@type': 'Reservation', name: 'Appointment request' },
  },
  sameAs: [
    business.instagram.url,
    business.bookingUrl,
    business.yelpUrl,
    gbpListingUrl(business.gbpPlaceId),
    'https://en.wikipedia.org/wiki/Day_spa',
  ],
  amenityFeature: [
    { '@type': 'LocationFeatureSpecification', name: 'Free parking', value: true },
    { '@type': 'LocationFeatureSpecification', name: 'By appointment only', value: true },
    { '@type': 'LocationFeatureSpecification', name: 'Private studio', value: true },
  ],
  availableService: [
    {
      '@type': 'MedicalProcedure',
      name: 'HydraFacial',
      url: absolute('treatments/hydrafacial', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Face',
      followup: 'Monthly treatments recommended for optimal skin health',
    },
    {
      '@type': 'MedicalProcedure',
      name: 'HydraFacial Keravive',
      url: absolute('treatments/keravive-scalp', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Scalp',
      followup: 'Series of three treatments recommended',
    },
    {
      '@type': 'MedicalProcedure',
      name: 'DMK Enzyme Therapy',
      url: absolute('treatments/dmk-enzyme-therapy', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Face',
      followup: 'Level 1 treatment required before advancing to deeper protocols',
    },
    {
      '@type': 'MedicalProcedure',
      name: 'Procell Microchanneling',
      url: absolute('treatments/procell-microchanneling', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Face',
      followup: 'Three treatment series spaced 4-6 weeks apart',
    },
    {
      '@type': 'MedicalProcedure',
      name: 'Lira Chemical Peels',
      url: absolute('treatments/lira-peels', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Face',
      followup: 'Series of treatments with home care between sessions',
    },
    {
      '@type': 'MedicalProcedure',
      name: 'Dermaplaning',
      url: absolute('treatments/dermaplaning', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Face',
      followup: 'Can be paired with chemical peels for enhanced results',
    },
    {
      '@type': 'MedicalProcedure',
      name: 'Acne Treatment',
      url: absolute('treatments/acne', site),
      procedureType: 'https://schema.org/NoninvasiveProcedure',
      medicineSystem: 'https://schema.org/WesternConventional',
      bodyLocation: 'Face',
      followup: 'Treatment protocol depends on acne phase and severity',
    },
  ],
  hasOfferCatalog: {
    '@type': 'OfferCatalog',
    name: 'Treatments',
    itemListElement: services.map((s) => {
      const itemOffered = {
        '@type': 'Service',
        name: s.name,
        url: absolute(s.url, site),
        provider: { '@id': entityId },
      };
      if (!s.priced) return { '@type': 'Offer', itemOffered };
      return {
        '@type': 'Offer',
        price: verifiedPrices[s.priced].replace('$', ''),
        priceCurrency: 'USD',
        itemOffered,
      };
    }),
  },
  makesOffer: [
    {
      '@type': 'Offer',
      itemOffered: {
        '@type': 'MedicalTherapy',
        name: 'Hyperpigmentation Treatment',
        description:
          'Professional treatment for melasma, dark spots, and post-inflammatory hyperpigmentation using chemical peels, microchanneling, and corrective facials.',
        relevantSpecialty: 'Dermatology',
        areaServed: business.schemaAreaServed.map((name) => ({ '@type': 'City', name })),
        provider: { '@id': entityId },
      },
      eligibleRegion: { '@type': 'State', name: 'California' },
    },
    {
      '@type': 'Offer',
      itemOffered: {
        '@type': 'MedicalTherapy',
        name: 'Melasma Treatment',
        description:
          'Advanced melasma treatment safe for all skin tones, including melanin-rich skin. Specialized protocols for Fitzpatrick Types IV-VI.',
        relevantSpecialty: 'Dermatology',
        areaServed: business.schemaAreaServed.map((name) => ({ '@type': 'City', name })),
        provider: { '@id': entityId },
      },
      eligibleRegion: { '@type': 'State', name: 'California' },
    },
    {
      '@type': 'Offer',
      itemOffered: {
        '@type': 'MedicalTherapy',
        name: 'Corrective Scalp Treatment',
        description:
          'Medical-grade scalp therapy for dryness, irritation, and scalp health using Keravive technology. Addresses hard water buildup common in San Diego.',
        relevantSpecialty: 'Dermatology',
        areaServed: business.schemaAreaServed.map((name) => ({ '@type': 'City', name })),
        provider: { '@id': entityId },
      },
      eligibleRegion: { '@type': 'State', name: 'California' },
    },
    {
      '@type': 'Offer',
      itemOffered: {
        '@type': 'MedicalTherapy',
        name: 'Acne Scarring Treatment',
        description:
          'Procell microchanneling and chemical peels for acne scar correction and texture improvement. Safe protocols for melanin-rich skin.',
        relevantSpecialty: 'Dermatology',
        areaServed: business.schemaAreaServed.map((name) => ({ '@type': 'City', name })),
        provider: { '@id': entityId },
      },
      eligibleRegion: { '@type': 'State', name: 'California' },
    },
  ],
  // NOTE: no aggregateRating (spec §2), no openingHoursSpecification (unverified).
};

/**
 * Separate block, not an @graph: the guard suite parses each JSON-LD script's
 * top-level @type to verify the NAP, and wrapping these in a graph would hide it.
 * No SearchAction — the site has no search endpoint and declaring one is a lie.
 */
const website = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  '@id': websiteId(site),
  url: home,
  name: business.name,
  description: 'Corrective facials and scalp therapy in Mission Hills, San Diego.',
  publisher: { '@id': entityId },
  inLanguage: 'en-US',
};
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(schema)} />
<script type="application/ld+json" is:inline set:html={JSON.stringify(website)} />
